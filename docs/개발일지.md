# 개발 일지

Plant Maintenance Copilot — SK그룹 해커톤 본선

기록 형식: **한 일 / 문제 / 조치 / 확인** 네 칸.
매일 3~5줄이면 충분하다. 문제가 있었던 날만 길게 쓴다.

---

## 2026-08-22 (토)

**작업자**: 임경민

**한 일**
- 본선 평가 기준·제출 양식 확인, 마감(9/3)까지 작업 순서 정리
- 공개 저장소에 실물 자료 파생물이 포함된 것을 발견하고 제거

**문제**

`.gitignore`가 `index/embeddings.npy`처럼 파일 단위로만 제외하고 있어서
폴더 자체가 열려 있었다. 그 결과 다음 두 가지가 Initial commit부터
공개 저장소에 올라가 있었다.

- `index/chunks.jsonl` — 실물 벤더 매뉴얼 42종에서 추출한 본문 650청크
- `derived/manual_index_cache.json` — 실물 매뉴얼 45종의 전체 경로와 폴더 구조

파일명 목록 중 하나에는 고객사명이 그대로 포함되어 있었다.
폴더 단위 제외 규칙은 `gitignore.txt`라는 별도 파일에 적혀 있었으나
`.gitignore`가 아니어서 git이 읽지 않았다.

**조치**
- `git filter-repo --path index/ --path derived/ --invert-paths` 로 전체 이력에서 제거
- 작업 전 폴더 전체 백업 (filter-repo는 워킹트리 파일도 함께 삭제함)
- `gitignore.txt` 내용을 `.gitignore`로 통합, 폴더 단위(`data/ index/ derived/`)로 변경
- force push 대신 저장소 삭제 후 재생성 — 도달 불가 객체가 커밋 SHA로 남는 경로까지 차단

**확인**
- `git ls-files index derived` → 결과 없음
- `git log --all --oneline -- index/ derived/` → 결과 없음
- `git grep -i hynix $(git rev-list --all)` → 결과 없음
- 커밋 수 8개 (기존 7 + 정리 커밋 1)
- GitHub 웹에서 파일 목록 및 저장소 검색으로 재확인

**남은 것**
- 도남진 재클론 필요 (이력이 갈림)
- `demo_data/manuals/` 벤더 PDF 3종은 유지 결정 — 제조사 공개 자료이며
  심사자가 클론 후 바로 실행할 수 있어야 하므로. README에 출처 명기 예정

---

## 2026-08-23 (일)

**작업자**:

**한 일**
-

**문제**

**조치**

**확인**
